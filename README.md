# Handwritten Digit Recognition

This project implements a handwritten digit recognition system using deep learning and the MNIST dataset.

The model was trained using TensorFlow and achieved around 97% accuracy.
